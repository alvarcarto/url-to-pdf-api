---

- hosts: localhost
  remote_user: travis
  sudo: true

  vars:
    rvm1_rubies:
      - 'ruby-{{ ruby_version }}'
    rvm1_install_path: '/home/travis/.rvm'
    rvm1_install_flags: '--auto-dotfiles --user-install'

  roles:
    - rvm1-ansible
